<form method="post" action="{{ path('app_hotel_delete', {'id_hotel': hotel.idHotel}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hotel.idHotel) }}">
    <button class="btn">Delete</button>
</form>
