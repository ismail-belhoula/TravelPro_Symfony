<form method="post" action="{{ path('app_commande_delete', {'id_commande': commande.id_commande}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande.id_commande) }}">
    <button class="btn">Delete</button>
</form>
