<form method="post" action="{{ path('app_hotel_delete', {'id_hotel': hotel.id_hotel}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hotel.id_hotel) }}">
    <button class="btn">Delete</button>
</form>
