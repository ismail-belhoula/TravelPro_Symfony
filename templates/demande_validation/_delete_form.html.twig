<form method="post" action="{{ path('app_demande_validation_delete', {'id': demande_validation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_validation.id) }}">
    <button class="btn">Delete</button>
</form>
