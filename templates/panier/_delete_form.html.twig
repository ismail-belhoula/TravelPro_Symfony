<form method="post" action="{{ path('app_panier_delete', {'id_panier': panier.id_panier}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ panier.id_panier) }}">
    <button class="btn">Delete</button>
</form>
